// Placeholder file to satisfy imports
// Since we're now using gRPC directly, this file serves as a compatibility layer
// In a real implementation, you might want to remove this file entirely

class ApiService {
  // This is a placeholder to satisfy existing imports
  // The actual API calls are now made through the gRPC client
}

export default new ApiService()